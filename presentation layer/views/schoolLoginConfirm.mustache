<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/schoolLoginConfirm/schoolLoginConfirm.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>School login confirmation</title>
</head>
<body>
    <script>
        window.onload = function() {
            var successMessage = '{{messages.success}}';
            if (successMessage != '') {
                document.getElementById('success-group').style.opacity = '1';
                document.getElementById('success').innerHTML = successMessage;
            }
            var errorMessage = '{{messages.error}}';
            if (errorMessage != '') {
                document.getElementById('error-group').style.opacity = '1';
                document.getElementById('errors').innerHTML = errorMessage;
            }
        }
    </script>
    <div class="success-container">
        <div class="alert alert-success alert-dismissible" role="alert" id="success-group" style="opacity: 0; transition: 1s ease;">
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            <p id="success"></p>
        </div>
    </div>
    <div class="error-container">
        <div class="alert alert-danger" role="alert" id="error-group" style="opacity: 0; transition: 1s ease;">
            <p id="errors"></p>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <img src="/images/school_login pic.png" alt="Three Kenyan high school boys, one with blazer, another with grey half sweater and another with just a white shirt.">
        </div>
        <div class="col">
            <h2>Login confirmation</h2>
            <p>A code has been sent to your email address.
                Enter the code that has been sent below.</p>
            <form action="schoolPage/{{school}}/{{email}}" method="post">
                <label for="code">Code:</label><br>
                <input type="text" name="code" required><br><br>
                <button>Confirm</button>
            </form>
        </div>
    </div>
</body>
</html>